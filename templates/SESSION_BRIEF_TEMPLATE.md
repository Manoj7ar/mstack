# Session brief

_Durable handoff for the next chat or agent. Copy to repo root as `SESSION_BRIEF.md` (tracked or gitignored per team policy). Starter: duplicate this file and fill._

**Updated:** YYYY-MM-DD

## Goal

One sentence.

## Non-goals

What this session will **not** do.

## Branch / commit

`branch-name` @ `abc1234` (or “clean main”)

## Plan

Path to saved plan, e.g. `docs/plans/foo.md` or “inline in last message — summarize below.”

## Last command / how to verify

e.g. `npm test`, `npm run lint`, URL, or “not run yet.”

## Open questions

Blocking decisions only.

## Next agent

Concrete next step (one paragraph).

## Context

- **Project memory:** `docs/PROJECT_MEMORY.md` (read before UI/product work)
- **Debug handoff:** use `templates/DEBUG_SESSION_TEMPLATE.md` if mid-debug
