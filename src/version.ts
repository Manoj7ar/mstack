/** Repository / workflow pack name (npm workspace, GitHub repo). */
export const REPO_PRODUCT_NAME = "mstack";

/** HTTP reference service id returned by GET /v1/meta and OpenAPI title. */
export const SERVICE_NAME = "mstack-ideas-api";

/** Bumped when HTTP API behavior or routes change in a user-visible way. */
export const API_VERSION = "0.4.0";
